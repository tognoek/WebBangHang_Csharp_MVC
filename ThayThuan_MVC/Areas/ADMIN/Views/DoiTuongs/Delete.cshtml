@model ThayThuan_MVC.DoiTuong

@{
    ViewBag.Title = "Delete";
}
<div class="modal" style="position: relative;display: block;z-index: 0;overflow-y: hidden;">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Detail</h5>
            </div>
            <div class="modal-body">
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th scope="col">ID Objects Of Use</th>
                                <th>@Model.ID</th>
                            </tr>
                            <tr>
                                <th scope="col">User Objects Of Use</th>
                                <th>@Model.TenDoiTuong</th>
                            </tr>
                        </thead>
                    </table>
                </div>

                <div class="modal-footer">
                    @using (Html.BeginForm())
                    {
                        @Html.AntiForgeryToken()

                        <div class="form-actions no-color">
                            <a href="@Url.Action("Index", "DoiTuong")" class=" btn btn-default m-r-10">Close</a>
                            |
                            <button type="submit" class="btn btn-info m-r-10">
                                Delete
                            </button>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>