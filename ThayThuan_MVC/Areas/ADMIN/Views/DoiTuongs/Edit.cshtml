@model ThayThuan_MVC.DoiTuong

@{
    ViewBag.Title = "Edit";
}
<div class="modal" style="position: relative;display: block;z-index: 0;overflow-y: hidden;">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Edit For Objects Of Use ID : @Model.ID</h5>
            </div>
            <div class="modal-body">
                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()

                    <div class="form-horizontal">
                        <hr />
                        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                        @Html.HiddenFor(model => model.ID)
                        <div class="form-group">
                            @Html.LabelFor(model => model.TenDoiTuong, "User Objects Of Use", htmlAttributes: new { @class = "control-label col-md-4" })
                            <div class="col-md-10">
                                @Html.EditorFor(model => model.TenDoiTuong, new { htmlAttributes = new { @class = "form-control" } })
                            </div>
                        </div>

                        <br />
                        <div class="modal-footer">
                            <a href="@Url.Action("Index", "DoiTuongs")" class="btn btn-default m-r-10">Close</a>
                            |
                            <button type="submit" value="Save" class="btn btn-primary">Save changes</button>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>